#include<iostream>
using namespace std;
#include<string>
int main()
{
   char first[]="hello world";
   cout<<"string before strrev="<<first;
    strrev(first);
    cout<<"string before strrev=";
    puts(first);
    
   return 0;
}