#include <iostream>

using namespace std;

int main()
{
    cout<<"input no both are weather equal or not"<<endl;
    int a,b;
    cout<<"enter two no"<<endl;
    cout<<"a=";
    cin>>a;
    cout<<"b=";
    cin>>b;
    if(a==b)
    cout<<"both are equal no";
    if(a>b)
    cout<<"a is greater than b";
    if(a<b)
    cout<<"a is less than b";

    return 0;
}
